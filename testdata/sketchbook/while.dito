
collatz_seq = func(x) -> (
    x <- collatz(x) for x >= 10)

collatz_seq = (func(x) -> (
    [x <- (func(a) -> (
        a if a <= 1 else
        a * 2 if a % 2 == 0 else
        a * 3 - 1)(x) for x >= 10]))

double = func(xray) -> (
    [x * 2 for x in xray if x % 2 == 0])
