# Test for imports.
    alpha := iota(1, 11)
    hypot := func(a, b) -> sqrt(a**2 + b**2)

    alpha, beta, charlie: int = 1, 2, 3
    days : []tuple

    (a, b, c) = abc

    abc = (a, b, c) = (1, 2, 3)

    a = (1, 2, 3)
    a = 1, 2, 3
    a = {1, 2, 2, 3}
    a = [1, 2, 3]

    func saturday(days) : int {
        return index(days, "saturday")
    }